$(function(){
	
	// Checking for CSS 3D transformation support
	$.support.css3d = supportsCSS3D();
	
	
	
	// efecto para un cuadrado incio
	
	var formContainer = $('#formContainer');
	
	// Listening for clicks on the ribbon links
	$('.flipLink').click(function(e){
		
		// Flipping the forms
		formContainer.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login-calvin').toggle();
		}
		e.preventDefault();
	});
	
	formContainer.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});

	//final de efecto para un cuadrado
	
	
	
var formContainer2 = $('#formContainer2');
	
	// Listening for clicks on the ribbon links
	$('.flipLink2').click(function(e){
		
		// Flipping the forms
		formContainer2.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login-david').toggle();
		}
		e.preventDefault();
	});
	
	formContainer2.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	
	
	
	var formContainer3 = $('#formContainer3');
	
	// Listening for clicks on the ribbon links
	$('.flipLink3').click(function(e){
		
		// Flipping the forms
		formContainer3.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login-infected').toggle();
		}
		e.preventDefault();
	});
	
	formContainer3.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	
	
	var formContainer4 = $('#formContainer4');
	
	// Listening for clicks on the ribbon links
	$('.flipLink4').click(function(e){
		
		// Flipping the forms
		formContainer4.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login-kaden').toggle();
		}
		e.preventDefault();
	});
	
	formContainer4.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});



var formContainer5 = $('#formContainer5');
	
	// Listening for clicks on the ribbon links
	$('.flipLink5').click(function(e){
		
		// Flipping the forms
		formContainer5.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer5.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});



var formContainer6 = $('#formContainer6');
	
	// Listening for clicks on the ribbon links
	$('.flipLink6').click(function(e){
		
		// Flipping the forms
		formContainer6.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer6.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	
	
	var formContainer7 = $('#formContainer7');
	
	// Listening for clicks on the ribbon links
	$('.flipLink7').click(function(e){
		
		// Flipping the forms
		formContainer7.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer7.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});


var formContainer8 = $('#formContainer8');
	
	// Listening for clicks on the ribbon links
	$('.flipLink8').click(function(e){
		
		// Flipping the forms
		formContainer8.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer8.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	
	var formContainer9 = $('#formContainer9');
	
	// Listening for clicks on the ribbon links
	$('.flipLink9').click(function(e){
		
		// Flipping the forms
		formContainer9.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer9.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	var formContainer10 = $('#formContainer10');
	
	// Listening for clicks on the ribbon links
	$('.flipLink10').click(function(e){
		
		// Flipping the forms
		formContainer10.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer10.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	
	var formContainer11 = $('#formContainer11');
	
	// Listening for clicks on the ribbon links
	$('.flipLink11').click(function(e){
		
		// Flipping the forms
		formContainer11.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer11.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	var formContainer12 = $('#formContainer12');
	
	// Listening for clicks on the ribbon links
	$('.flipLink12').click(function(e){
		
		// Flipping the forms
		formContainer12.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer12.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	var formContainer13 = $('#formContainer13');
	
	// Listening for clicks on the ribbon links
	$('.flipLink13').click(function(e){
		
		// Flipping the forms
		formContainer13.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer13.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	var formContainer14 = $('#formContainer14');
	
	// Listening for clicks on the ribbon links
	$('.flipLink14').click(function(e){
		
		// Flipping the forms
		formContainer14.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer14.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	var formContainer15 = $('#formContainer15');
	
	// Listening for clicks on the ribbon links
	$('.flipLink15').click(function(e){
		
		// Flipping the forms
		formContainer15.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer15.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	var formContainer16 = $('#formContainer16');
	
	// Listening for clicks on the ribbon links
	$('.flipLink16').click(function(e){
		
		// Flipping the forms
		formContainer16.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer16.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	var formContainer17 = $('#formContainer17');
	
	// Listening for clicks on the ribbon links
	$('.flipLink17').click(function(e){
		
		// Flipping the forms
		formContainer17.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer17.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});

	var formContainer18 = $('#formContainer18');
	
	// Listening for clicks on the ribbon links
	$('.flipLink18').click(function(e){
		
		// Flipping the forms
		formContainer18.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer18.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});
	
	var formContainer19 = $('#formContainer19');
	
	// Listening for clicks on the ribbon links
	$('.flipLink19').click(function(e){
		
		// Flipping the forms
		formContainer19.toggleClass('flipped');
		
		// If there is no CSS3 3D support, simply
		// hide the login form (exposing the recover one)
		if(!$.support.css3d){
			$('#login').toggle();
		}
		e.preventDefault();
	});
	
	formContainer19.find('form').submit(function(e){
		// Preventing form submissions. If you implement
		// a backend, you might want to remove this code
		e.preventDefault();
	});












	
	// A helper function that checks for the 
	// support of the 3D CSS3 transformations.
	function supportsCSS3D() {
		var props = [
			'perspectiveProperty', 'WebkitPerspective', 'MozPerspective'
		], testDom = document.createElement('a');
		  
		for(var i=0; i<props.length; i++){
			if(props[i] in testDom.style){
				return true;
			}
		}
		
		return false;
	}
});
